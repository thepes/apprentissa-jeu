<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.copains.ludroid"
    android:installLocation="auto"
    android:versionCode="400002"
    android:versionName="0.2.1" >

    <uses-sdk android:minSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:icon="@drawable/findnumber_btn"
        android:label="@string/app_name" >
        <activity
            android:name=".LudroidMainActivity"
            android:label="@string/app_name" >
            
        </activity>
        <activity android:name=".account.activities.AccountCreationActivity" >
        </activity>
        <activity android:name=".games.findnumbers.FindNumberActivity" >
        </activity>
        <activity android:name=".games.findcolors.FindColorsActivity" >
        </activity>
        <activity android:name=".games.maze.MazeActivity" >
        </activity>
        <activity android:name=".games.findshape.FindShapeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>